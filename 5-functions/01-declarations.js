/*
    FUNCTIONS

    - functions are a chunk of code that perform a set of actions when invoked
    - functions are reusable; we can call them as many times as we want and they can take in different sets of data
    
*/

function hi() {
    console.log('hello!');
}

hi();
